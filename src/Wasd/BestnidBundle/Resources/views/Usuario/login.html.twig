{% extends "base.html.twig" %}

{% block contenido %}
    <form action="{{ path('usuario_login_check') }}" method="post">
        <h3 class="titulo col-md-offset-3">Iniciar sesión</h3>
        <div class="row">
            <div class="form-group col-md-3 col-md-offset-3">
                <label for="username">Usuario:</label>
                <div class="input-group col-md-12">
                    <input type="text" id="username" name="_username" value="{{ last_username | default('') }}" class="form-control" />
                </div>
            </div>
            <div class="form-group col-md-3">    
                <label for="password">Contraseña:</label>
                <div class="input-group col-md-12">
                    <input type="password" id="password" name="_password" class="form-control" />
                </div>
            </div>
        </div>
        {% if error %}
            <div class="row">
                <div class="text-danger col-md-3 col-md-offset-3">
                    Usuario y/o contraseña incorrecta.
                </div>
            </div>
        {% endif %}
        <div class="row">
            <div class="form-group col-md-6 col-md-offset-3">
                <div class="input-group col-md-12">
                    <label for="no_cerrar" class="container-fluid">No cerrar sesión</label>
                    <input type="checkbox" id="no_cerrar" name="_remember_me" checked />
                </div>
            </div>
        </div>
        
        <div class="row">
            <div class="form-group col-md-6 col-md-offset-3">
                <input type="submit" name="login" value="Acceder" class="btn btn-success" />
            </div>
        </div>
    </form>
        
{% endblock %}